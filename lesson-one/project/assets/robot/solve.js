function solve(){

  var instructions = "\n\nSolve a maze with your robot!\nFirst, refresh the page until you find a maze you like.\n";
  instructions += "Then, type `maze.save()` in your console.  Your maze will be saved!\n\n";
  instructions += "Finally, write your code in `project/assets/robot/solve.js` for solving the maze!";

  return instructions;
}